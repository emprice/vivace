{
  "name": "vivace-client",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "eslint": "npx eslint . --cache --ext .js --fix",
    "prettier": "npx prettier \"**/*{.ts,.tsx,.js,.jsx,.html,.css,.json,.md}\" --write --list-different",
    "stylelint": "npx stylelint --cache \"**/*.css\" --fix",
    "lint": "npm run stylelint && npm run prettier && npm run eslint",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ellen M. Price",
  "license": "MIT",
  "dependencies": {
    "socket.io-client": "^4.5.1"
  },
  "devDependencies": {
    "eslint": "^8.16.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-prettier": "^4.0.0",
    "eslint-plugin-security-node": "^1.1.1",
    "prettier": "^2.6.2",
    "stylelint": "^14.8.3",
    "stylelint-config-prettier": "^9.0.3",
    "stylelint-config-recommended": "^7.0.0",
    "stylelint-config-standard": "^25.0.0",
    "stylelint-prettier": "^2.0.0"
  }
}
